[project]
name = "prepsync-backend"
version = "0.1.0"
description = "PrepSync backend service"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]
