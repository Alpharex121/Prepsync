# Phase 1: Project Setup & Foundation

- [x] Create monorepo/app structure (`backend`, `frontend`, `shared`).
- [x] Setup FastAPI app with environment-based config.
- [x] Setup React app with routing and base layout.
- [x] Configure PostgreSQL and Redis local/dev connections.
- [x] Add Docker Compose for local dependencies (Postgres, Redis).
- [x] Add lint/format tools (Python + JS/TS) and pre-commit hooks.
- [x] Setup CI pipeline for lint + basic tests.
