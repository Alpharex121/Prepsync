# Phase 3: Room Lifecycle (Core Backend)

- [x] Implement room creation API.
- [x] Implement room join-check API (`LOBBY` only).
- [x] Implement Redis room state keys (`status`, `config`, `questions`, `leaderboard`).
- [x] Implement room state transitions (`LOBBY -> GENERATING -> ACTIVE -> FINISHED`).
- [x] Add late-join guard middleware for WebSocket connections.
